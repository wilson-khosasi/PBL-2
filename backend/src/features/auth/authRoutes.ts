import express from 'express';
import type { Router } from 'express';

const router: Router = express.Router();

// Authentication endpoints are added one at a time in the next phases.
export default router;
